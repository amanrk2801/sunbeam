#include <iostream>

int main()
{
    bool status = 0;
    // any non zero number is considered as true

    printf("Value in status = %d\n", status);
    printf("size of status = %d\n", sizeof(status));
    return 0;
}